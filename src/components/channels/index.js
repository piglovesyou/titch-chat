import React from 'react';
import s from './index.sass';

export default function(props) {
  return (
    <ul className={s.root}>
      <li className={s.item}>a</li>
      <li className={s.item}>a</li>
      <li className={s.item}>a</li>
    </ul>
  );
};
